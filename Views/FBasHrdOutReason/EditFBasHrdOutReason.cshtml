@model FBasHrdOutReasonViewModel
<h2><i class="fa fa-edit text-warning" aria-hidden="true"></i> Edit @ViewData["Title"]</h2>
<hr />

<div class="container-fluid">
    <form id="form" role="form" asp-action="EditFBasHrdOutReason">
        <div class="panel panel-default setup-content">
            @Html.HiddenFor(d => d.FBasHrdOutReason.EncryptedId)
            @Html.HiddenFor(d => d.FBasHrdOutReason.RESASON_NAME)
            <div class="panel-body row">
                <div class="col-lg-2 col-md-3 col-sm-6 col-xs-12">
                    <div class="form-group">
                        <label asp-for="FBasHrdOutReason.RESASON_NAME" class="control-label"></label>
                        <input disabled="disabled" asp-for="FBasHrdOutReason.RESASON_NAME" class="form-control" />
                        <span asp-validation-for="FBasHrdOutReason.RESASON_NAME" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-lg-2 col-md-3 col-sm-6 col-xs-12">
                    <div class="form-group">
                        <label asp-for="FBasHrdOutReason.REMARKS" class="control-label"></label>
                        <input asp-for="FBasHrdOutReason.REMARKS" class="form-control" />
                        <span asp-validation-for="FBasHrdOutReason.REMARKS" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                    <button class="btn btn-warning pull-right" type="submit">Update</button>
                    <a asp-action="GetFBasHrdOutReason" class="btn btn-dark pull-right">Back to List</a>
                </div>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync($"_ValidationScriptPartial_Except_jQuery");}
}